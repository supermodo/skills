---
skill: tests
status: needs_input
summary: "Mistyped status: must NOT pass as a healthy row."
---

A status outside the four documented values. `needsYou` matches those strings
exactly, so passing this through would silently drop the run out of the alert
surface — the renderer marks it unreadable instead.
