---
skill: hunt
status: ok
summary: "Four findings, two confirmed by both models, two refuted."
task: csv-export
---

Confirmed findings, most severe first.

1. Null watermark crashes the incremental path (`packages/data/src/watermark.ts:42`)
2. Export writes **before** the schema check, so a bad column reaches disk

Refuted: the "unbounded memory" claim (the writer streams) and a *suspected*
race in the queue (single consumer by construction).

```mermaid
flowchart LR
  A[read] --> B[validate]
  B --> C[write]
```
